import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
  spinner: {
    position: 'absolute',
    zIndex: 999,
    padding: 10,
    alignSelf: 'center'
  }
})

export default styles
